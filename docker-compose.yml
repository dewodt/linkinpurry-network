services:
  # Database
  linkinpurry-db:
    container_name: linkinpurry-db
    image: postgres:17
    env_file:
      - ./backend/.env.docker
    ports:
      - "5436:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
      # - ./backend/src/infrastructures/database/migrations:/docker-entrypoint-initdb.d
    networks:
      - linkinpurry-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB} -h ${POSTGRES_HOST} -p ${POSTGRES_PORT}",
        ]

  # BE Development (with hot reload)
  linkinpurry-be-dev:
    container_name: linkinpurry-be-dev
    build:
      context: .
      target: development
      dockerfile: ./backend/Dockerfile.run
    env_file:
      - ./backend/.env.docker
    volumes:
      - ./backend:/app # Mount the backend folder to the container
      - /app/node_modules # Prevent the node_modules folder from being mounted
    ports:
      - "3000:3000"
    depends_on:
      - linkinpurry-db
    networks:
      - linkinpurry-network

  # BE Production (no hot reload)
  linkinpurry-be-prod:
    container_name: linkinpurry-be-prod
    build:
      context: .
      target: production
      dockerfile: ./backend/Dockerfile.run
    env_file:
      - ./backend/.env.docker
    ports:
      - "3000:3000"
    depends_on:
      - linkinpurry-db
    networks:
      - linkinpurry-network

  # BE development (with hot reload)
  linkinpurry-fe-dev:
    container_name: linkinpurry-fe-dev
    build:
      context: .
      target: development
      dockerfile: ./frontend/Dockerfile
    env_file:
      - ./frontend/.env.docker
    volumes:
      - ./frontend:/app # Mount the frontend folder to the container
      - /app/node_modules # Prevent the node_modules folder from being mounted
    ports:
      - "5173:5173"
    networks:
      - linkinpurry-network

  # BE production (no hot reload)
  linkinpurry-fe-prod:
    container_name: linkinpurry-fe-prod
    build:
      context: .
      target: production
      dockerfile: ./frontend/Dockerfile
    env_file:
      - ./frontend/.env.docker
    ports:
      - "5173:5173"
    networks:
      - linkinpurry-network

volumes:
  db-data:
    name: linkinpurry-db-data

networks:
  linkinpurry-network:
    driver: bridge

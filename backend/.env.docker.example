# USED FOR DOCKER ENV
# e.g. running the app (dev/prod) in a container

# App
PORT=3000

# Database
POSTGRES_USER=linkinpurry
POSTGRES_PASSWORD=12345
POSTGRES_DB=linkinpurry
POSTGRES_HOST=linkinpurry-db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Add more